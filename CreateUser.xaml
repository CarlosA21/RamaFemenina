<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RamaFemenina.CreateUser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RamaFemenina"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Gestión de Usuarios">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="40,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="8">
                    <TextBlock Text="Gestión de Usuarios" Style="{StaticResource TitleTextBlockStyle}" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                    <TextBlock Text="Administre las cuentas de acceso al sistema" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Cerrar" Click="BtnCerrar_Click" Style="{StaticResource AccentButtonStyle}"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Padding="40,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="400"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Panel Izquierdo: Crear Usuario -->
            <Border Grid.Column="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="24">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="20">
                        <StackPanel Spacing="8">
                            <TextBlock Text="➕ Crear Nuevo Usuario" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                            <TextBlock Text="Complete los campos para crear una nueva cuenta" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        </StackPanel>

                        <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}"/>

                        <StackPanel Spacing="8">
                            <TextBlock Text="Nombre de Usuario" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <TextBox x:Name="txtNuevoUsuario" PlaceholderText="Ingrese el nombre de usuario" Height="40" MaxLength="50" TextChanged="ValidarFormulario"/>
                            <TextBlock x:Name="txtUsuarioError" Text="⚠️ El nombre de usuario es obligatorio" Foreground="Red" Style="{StaticResource CaptionTextBlockStyle}" Visibility="Collapsed"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="Contraseña" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <PasswordBox x:Name="pwdNuevaContraseña" PlaceholderText="Ingrese la contraseña" Height="40" MaxLength="100" PasswordChanged="ValidarFormulario"/>
                            <TextBlock x:Name="txtContraseñaError" Text="⚠️ La contraseña debe tener al menos 6 caracteres" Foreground="Red" Style="{StaticResource CaptionTextBlockStyle}" Visibility="Collapsed"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="Confirmar Contraseña" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <PasswordBox x:Name="pwdConfirmarContraseña" PlaceholderText="Confirme la contraseña" Height="40" MaxLength="100" PasswordChanged="ValidarFormulario"/>
                            <TextBlock x:Name="txtConfirmarError" Text="⚠️ Las contraseñas no coinciden" Foreground="Red" Style="{StaticResource CaptionTextBlockStyle}" Visibility="Collapsed"/>
                        </StackPanel>

                        <InfoBar Severity="Informational" IsOpen="True" IsClosable="False" Title="Seguridad" Message="Las contraseñas se encriptan con BCrypt. Use al menos 8 caracteres."/>

                        <Button x:Name="btnCrearUsuario" Content="Crear Usuario" Click="BtnCrearUsuario_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Height="44" IsEnabled="False"/>

                        <TextBlock x:Name="txtMensajeCreacion" TextWrapping="Wrap" FontWeight="SemiBold" HorizontalAlignment="Center" Visibility="Collapsed"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Panel Derecho: Lista de Usuarios -->
            <Border Grid.Column="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
                        <TextBlock Text="👥 Usuarios Registrados" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                        <TextBlock Text="Administre los usuarios del sistema" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>

                    <AutoSuggestBox Grid.Row="1" x:Name="SearchBox" PlaceholderText="🔍 Buscar usuario..." QueryIcon="Find" Height="40" Margin="0,0,0,16" TextChanged="SearchBox_TextChanged"/>

                    <ListView Grid.Row="2" x:Name="UsuariosListView" SelectionMode="Single" SelectionChanged="UsuariosListView_SelectionChanged">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="12" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <PersonPicture Grid.Column="0" Width="40" Height="40" Margin="0,0,12,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                        <TextBlock Text="{Binding Usuario}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                                        <TextBlock Text="{Binding FechaCreacion}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Spacing="4">
                                        <FontIcon Glyph="{Binding IconoSeguridad}" FontSize="20" Foreground="{Binding ColorSeguridad}"/>
                                        <TextBlock Text="{Binding EstadoSeguridad}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,0,8"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>

                    <CommandBar Grid.Row="3" DefaultLabelPosition="Right" Margin="0,16,0,0">
                        <AppBarButton Icon="Refresh" Label="Actualizar" Click="BtnActualizar_Click"/>
                        <AppBarButton x:Name="btnCambiarContraseña" Icon="Edit" Label="Cambiar Contraseña" Click="BtnCambiarContraseña_Click" IsEnabled="False"/>
                        <AppBarButton x:Name="btnEliminarUsuario" Icon="Delete" Label="Eliminar" Click="BtnEliminarUsuario_Click" IsEnabled="False"/>
                    </CommandBar>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
